const endPoints = {
    metadata: "/metadata",
    googleAnalytics: "/search-console",
    robotTxt: "/robotTxt",
    siteMap: "/siteMap",
};

export const { metadata, googleAnalytics, robotTxt, siteMap } = endPoints;
